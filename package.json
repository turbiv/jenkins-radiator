{
  "name": "jenkins-radiator-backend",
  "license": "MIT",
  "engines": {
    "node": "^14.8.0",
    "yarn": "^1.22.4"
  },
  "scripts": {
    "build": "yarn --cwd backend install",
    "heroku-postbuild": "yarn build"
  }
}